{
  "name": "shared",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "cleanbuild": "tsc --build --clean && tsc --watch",
    "build:watch": "tsc --watch",
    "build": "tsc",
    "compile_deamon": "yarn build",
    "clean": "rm -rf node_modules/",
    "clean:dist": "rm -rf dist/ && rm tsconfig.tsbuildinfo || true"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {}
}
